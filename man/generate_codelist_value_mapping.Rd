% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/advanced_mapping.R
\name{generate_codelist_value_mapping}
\alias{generate_codelist_value_mapping}
\title{Generate Codelist Value Mapping Script}
\usage{
generate_codelist_value_mapping(
  source_values,
  target_codelist,
  source_column_name,
  target_codelist_name,
  llm_config,
  matching_threshold = 0.7,
  include_fuzzy_matching = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{source_values}{Vector of unique values from the source data column}

\item{target_codelist}{Data frame with codelist containing code_id and optional name/description columns}

\item{source_column_name}{Character. Name of the source column for context}

\item{target_codelist_name}{Character. Name/ID of the target codelist}

\item{llm_config}{LLM configuration from \code{create_llm_config()}}

\item{matching_threshold}{Numeric. Minimum similarity threshold for automatic matching (0-1, default: 0.7)}

\item{include_fuzzy_matching}{Logical. Include fuzzy matching logic in generated script (default: TRUE)}

\item{verbose}{Logical. Print detailed information (default: FALSE)}
}
\value{
Character string containing R code for value mapping
}
\description{
Uses LLM to generate R code for mapping specific data column values to SDMX codelist codes.
This is a focused approach that targets value transformation rather than full structure mapping.
}
\examples{
\dontrun{
# Example: Map country names to ISO codes
source_values <- c("United States", "Canada", "Mexico")
target_codelist <- data.frame(
  code_id = c("US", "CA", "MX"),
  name = c("United States", "Canada", "Mexico")
)

config <- create_llm_config("ollama", "llama2")
mapping_script <- generate_codelist_value_mapping(
  source_values, target_codelist, "country_name", "ISO_COUNTRY", config
)
cat(mapping_script)
}
}